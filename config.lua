Config = {}

Config.ServerPrint = false --This will determine if things will print to your server log. Only recommend this if you are having problems with the script

-- Make sure you create the storage in your database (addon_inventory)
Config.Locations = {
    {
        label = 'Bennys Automotive',
        name = 'society_bennys',
        coord = vector3(-206.8177, -1331.429, 34.89)
    }
}